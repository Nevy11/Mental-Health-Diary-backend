// use crate::{connection::establish_connection::establish_connection, models::Question};

// pub fn insert_questions(questions: Vec<(String, String)>) {
//     let new_questions: Vec<Question> = questions
//         .into_iter()
//         .enumerate()
//         .map(|(i, (question, context))| Question { question, context })
//         .collect();
//     let connection = &mut establish_connection();
// }
